export default `/**
 * This object represents the props that will be injected
 * into <%= name%> in the test phase.
 * sometimes is useful to have more than one scenario
 * and keep the default one as the most common case.
 */
export default {}`